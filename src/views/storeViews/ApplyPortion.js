import React, { useEffect, useState } from "react";
import { makeStyles } from "@material-ui/styles";

const useStyles = makeStyles((theme) => ({}));
function ApplyPortion(props) {
  const classes = useStyles(props);

  return <>ApplyPortion</>;
}

export default ApplyPortion;
